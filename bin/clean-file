#!/bin/bash
umask 0077
shopt -s nullglob
set -euo pipefail

if [ $# = 0 ]; then
  echo 'Usage: clean-file FILE [FILE ...]'
  exit 1
fi

for file in "$@" ; do
  chmod 600 "$file"
  xattr -c "$1" >/dev/null 2>&1 || true
  chmod 400 "$file"
done