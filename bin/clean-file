#!/bin/bash
umask 0077
set -euo pipefail
shopt -s nullglob

if [ $# = 0 ]; then
  echo 'Usage: clean-file FILE [FILE ...]'
  exit 1
fi

for file in "$@" ; do
  chmod 600 "$file"
  xattr -c "$file"
  chmod 400 "$file"
done