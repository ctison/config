#!/bin/bash
umask 0077
set -euo pipefail

OUTPUT=${1:-/dev/stdout}

cat > "$OUTPUT" <<EOF
import React from 'react
import { NextPage } from 'next'
EOF

if test -f "$OUTPUT" && type code >/dev/null >&1 ; then
  code "$OUTPUT"
fi