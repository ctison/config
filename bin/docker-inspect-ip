#!/bin/bash
umask 0077
set -euo pipefail
shopt -s nullglob

if [ $# -ne 1 ]; then
	echo "Usage: $0 CONTAINER"
	exit
fi

CONTAINER="$1"
set -x
exec docker inspect "$CONTAINER" -f '{{.NetworkSettings.IPAddress}}'