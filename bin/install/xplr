#!/bin/bash
umask 0077
shopt -s nullglob
set -euxo pipefail

curl -fsSLO https://github.com/sayanarijit/xplr/releases/latest/download/xplr-linux.tar.gz
tar --no-same-{o,p} -C /usr/local/bin/ -xf xplr-linux.tar.gz xplr
rm xplr-linux.tar.gz
chmod 500 /usr/local/bin/xplr
