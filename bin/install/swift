#!/bin/sh
umask 0077
set -ex

# Swift
# ARG SWIFT_VERSION='5.1'
# RUN curl -O https://swift.org/builds/swift-${SWIFT_VERSION}-release/ubuntu1804/swift-${SWIFT_VERSION}-RELEASE/swift-${SWIFT_VERSION}-RELEASE-ubuntu18.04.tar.gz
# RUN mkdir /usr/local/swift
# RUN ${TAR} -xf /swift-${SWIFT_VERSION}-RELEASE-ubuntu18.04.tar.gz -C /usr/local/swift --strip-components=1
# RUN rm -- swift-${SWIFT_VERSION}-RELEASE-ubuntu18.04.tar.gz
# # RUN apt-get install -y clang libicu-dev libcurl4-openssl-dev libpython2.7
# RUN apt-get install -y libcurl4-openssl-dev libpython2.7
# RUN ln -st /usr/local/bin /usr/local/swift/usr/bin/*