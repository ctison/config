#!/bin/bash
umask 0077
shopt -s nullglob
set -euxo pipefail

curl -Lo /usr/local/bin/websocat https://github.com/vi/websocat/releases/latest/download/websocat_amd64-linux-static
chmod 500 /usr/local/bin/websocat